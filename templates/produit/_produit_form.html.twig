{{ form_start(produit_form) }}

    {{ form_row(produit_form.nom, {label: "Nom du produit:"}) }}

    {{ form_row(produit_form.Description, {
        label: "Description:",
        help: "Ce champs est facultatif"
    }) }}
    
    {{ form_row(produit_form.prix, {label: "Prix (en euros):"}) }}

    {{ form_row(produit_form.quantite, {label: "Quantité en stock:"}) }}

    <button type="submit" class="btn btn-success">Enregistrer</button>

{{ form_end(produit_form) }}